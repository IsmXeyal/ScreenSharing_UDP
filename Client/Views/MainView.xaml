<Window x:Class="Client.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d" WindowStartupLocation="CenterScreen"
        Title="Screen Sharing" Height="700" Width="1000" MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Grid Background="#FF3A4F63">
        <Grid.RowDefinitions>
            <RowDefinition Height="16*"/>
            <RowDefinition Height="1.5*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="Black">
            <Grid Height="560" Width="940" Background="#FF191919">
                <Image x:Name="ImageShare"/>
            </Grid>
        </Grid>
        <Button Grid.Row="1" Height="35" Width="120" x:Name="mybtn" Content="Start" Foreground="White" FontSize="18" FontWeight="DemiBold"
            Background="#FF035203" BorderBrush="Black" BorderThickness="2" Command="{Binding StartCommand}" Padding="0 0 0 4" HorizontalAlignment="Center" Margin="0,0,0,-4" />

    </Grid>
</Window>
